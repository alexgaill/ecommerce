{% extends 'base.html.twig' %}


{% block body %}
    <div class="page-top-info">
        <div class="container">
        <p class="text-center h4">Connexion</p>
        </div>
    </div>
    <div class="container spad text-center">
        <form action="{{ path('login')}}" method="POST" class="flex">
            <div class="form-group column">
                <input type="email" required placeholder="Votre email" name="_username">   
            </div>
            <div class="form-group column">
                <input type="password" required placeholder="Votre mot de passe" name="_password">   
            </div>
            <div class="form-group">
                <button class="btn btn-primary">Se connecter</button>   
            </div>
            <div class="form-group">
                <p><a href="{{ path('forgetPassword') }}">Mot de passe oublié?</a></p>
            </div>
        </form>
    </div>
{% endblock %}
